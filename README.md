# Vibe-Coding Project Template

一套**结构化 AI 协作开发框架**，通过"主 Agent + Subagent"分层架构，实现高质量、可追溯的 AI 辅助软件开发。

## 🎯 为什么需要这个框架？

### 传统 AI 辅助开发的痛点

- ❌ **缺乏规划**：AI 直接写代码，缺乏系统设计
- ❌ **难以追溯**：不知道 AI 为什么这么做，无法复盘
- ❌ **质量不可控**：代码质量依赖 AI 的"心情"
- ❌ **返工成本高**：发现问题需要重新生成大量代码

### 我们的解决方案

✅ **分层协作**：主 Agent 负责规划和验收，Subagent 负责执行
✅ **全程留档**：每次调用都有完整的审计记录（request → response → info → stderr）
✅ **标准输出**：结构化 JSON 格式，便于程序化处理和审查
✅ **质量保证**：严格的验收标准，不合格就返工

## 🏗️ 核心架构

```
┌─────────────┐      委派任务      ┌──────────────┐
│  主 Agent   │ ────────────────> │  Subagent    │
│ (规划者)    │ <──────────────── │  (执行者)    │
└─────────────┘      反馈结果      └──────────────┘
      │                                 │
      │                                 │
      ↓                                 ↓
  创建 request.json                  生成 response.json
  验收 response.json                 记录执行过程
```

### 三层组织结构

| 层级 | 含义 | 典型规模 |
|------|------|---------|
| **Phase** | 一轮对话/一个目标 | 2-5 个 tasks |
| **Task** | 一个独立的开发任务 | 可能需要 1-3 个 subagents |
| **Subagent** | 一次 Agent 调用 | 单次执行 |

### 核心文件

每次 Subagent 调用都会生成以下文件：

```
.agent/audit/phase-001/task-001/subagent-001/
├── request.json    # 任务委派（你想要什么）
├── response.json   # 执行结果（AI 做了什么）
├── info.json       # 运行时数据（模型、token、时间）
└── stderr.txt      # 执行过程（完整对话记录）
```

## 📂 项目结构

```
.
├── .agent/                    # 框架核心
│   ├── docs/                  # Agent 行为规范
│   │   ├── Agent.md           # 主 Agent 必读（8k tokens，详细）
│   │   ├── subagent_config.md # 模型配置说明
│   │   └── subagent_prompt.md # Subagent 行为指令
│   ├── templates/             # JSON 模板
│   │   ├── template_request.json
│   │   └── template_response.json
│   ├── tools/                 # Subagent 工具
│   │   ├── run_subagent.py    # 主入口脚本
│   │   └── lib/               # 核心模块
│   └── audit/                 # 执行历史（自动生成）
│       └── phase-xxx/
│           └── task-xxx/
│               └── subagent-xxx/
│                   ├── request.json
│                   ├── response.json
│                   ├── info.json
│                   └── stderr.txt
├── docs/                      # 项目文档
│   ├── ToDo.md                # 任务清单（按 phase → task 组织）
│   ├── 项目需求文档.md
│   └── 项目设计文档.md
├── src/                       # 项目源码（AI 生成）
└── README.md                  # 本文档
```

## 🚀 快速开始

### 前置条件

- Python 3.8+
- 选择一个执行引擎：
  - **Codex CLI**（推荐新手）：需要安装 Codex CLI 工具，配置 `~/.codex/config.toml`
  - **Claude API**（推荐进阶）：需要 `ANTHROPIC_API_KEY` 环境变量

### 基础用法

```bash
# 使用 Codex CLI（适合简单任务，成本低）
python .agent/tools/run_subagent.py \
  --phase phase-001 \
  --task task-001 \
  --subagent subagent-001 \
  --engine codex \
  --profile deepseek-v3.2 \
  --sandbox workspace-write

# 使用 Claude API（适合复杂任务，质量高）
python .agent/tools/run_subagent.py \
  --phase phase-001 \
  --task task-001 \
  --subagent subagent-001 \
  --engine claude \
  --model claude-sonnet-4-5-20250929
```

更多参数和高级用法，请参考 [CLAUDE.md](CLAUDE.md)。

## 🔄 典型工作流

```
1. 准备阶段
   ├─ 明确需求和约束
   ├─ 技术选型（可选 2-3 个方案）
   └─ 编写 ToDo 清单（docs/ToDo.md）

2. 开发阶段（每个 Task）
   ├─ 创建 request.json
   ├─ 调用 Subagent 执行
   ├─ 验收 response.json
   └─ 失败则创建新 subagent 返工

3. 复盘阶段
   ├─ 查看 audit 目录了解执行过程
   ├─ 分析 token 消耗和时间成本
   └─ 总结经验和改进点
```

## 👥 角色职责

### 主 Agent（管理者）

- ✅ 拆解任务、制定验收标准
- ✅ 委派 Subagent 执行具体任务
- ✅ 验收结果并决定是否返工
- ❌ **禁止**直接编写或修改代码

### Subagent（执行者）

- ✅ 严格按 request.json 执行任务
- ✅ 输出结构化的 response.json
- ❌ **禁止**自作主张、偷工减料

### 人类（决策者）

- ✅ 明确需求和约束
- ✅ 在关键决策点介入（技术选型、架构设计）
- ✅ 复盘和改进工作流
- ✅ 处理 Agent 无法解决的异常情况

**何时需要人工介入？**

- 需求不明确时
- 多个技术方案需要选择时
- 发现风险或依赖不足时
- Agent 连续失败 3 次以上时

## 🎨 适用场景

### ✅ 推荐使用

- **新项目开发**：从零开始，快速搭建基础架构
- **功能迭代**：添加新功能、重构旧代码
- **代码审查**：系统性检查代码质量和安全问题
- **文档编写**：生成 API 文档、用户手册
- **测试编写**：单元测试、集成测试

### ⚠️ 谨慎使用

- **紧急修复**：返工成本可能超过直接修复
- **微调优化**：小改动可能不值得走完整流程
- **遗留系统**：需要大量上下文理解，可能效率低

## 💡 核心特性

### 1. 全程可追溯

每次 Subagent 调用都记录：

- **request.json**：你要什么
- **response.json**：AI 做了什么
- **info.json**：用了什么模型、消耗多少 token、花了多长时间
- **stderr.txt**：完整的执行过程（对话、工具调用、思考过程）

### 2. 双引擎支持

| 引擎 | 优势 | 劣势 | 适用场景 |
|------|------|------|---------|
| **Codex CLI** | 成本低、模型多样 | Token 不统计 | 简单任务、批量处理 |
| **Claude API** | 质量高、Token 可追踪 | 成本较高 | 复杂任务、关键功能 |

### 3. 严格的质量控制

- 每个 Task 必须有清晰的验收标准
- 不通过就返工，不偷工减料
- 完整的审计链，随时可复盘

## 🛠️ 故障排查

### Subagent 执行失败

1. **查看 stderr.txt**：了解 Agent 的执行过程和失败原因
2. **查看 info.json**：检查是否超时、是否异常退出
3. **检查 request.json**：任务描述是否清晰、验收标准是否明确
4. **调整后重试**：创建新的 subagent-xxx 重新执行

### 连续失败 3 次以上

- 检查任务是否过于复杂，考虑拆分
- 检查是否有依赖缺失（库、权限、配置）
- 考虑人工介入或更换模型

### Token 消耗过高

- 检查是否有不必要的上下文
- 考虑拆分成更小的任务
- 对于简单任务，使用 Codex CLI 代替 Claude API

## 📖 延伸阅读

- [Agent.md](.agent/docs/Agent.md)：主 Agent 必读，包含完整的操作规范和工作流程
- [CLAUDE.md](CLAUDE.md)：面向 AI 的项目说明，包含详细的命令和配置
- [.agent/docs/subagent_config.md](.agent/docs/subagent_config.md)：模型配置说明

## 🤝 贡献指南

这是一个模板项目，欢迎根据实际需求进行调整和扩展。

如果你有改进建议或发现问题，欢迎提出 Issue 或 Pull Request。

## 📄 许可证

MIT License
